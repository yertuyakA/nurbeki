import java.util.HashSet;
public class Hashset {

    /*public static void main(String[] args) {

    Set - множество - это такой же способ хранения данных, как массив или список.
    Но особенность множества в том, что оно может хранить только уникальные значения.
    HashSet хранит элементы в произвольном порядке, но зато быстро ищет. Подходит, если порядок Вам не важен, но важна скорость.
    Более того, для оптимизации поиска, HashSet будет хранить элементы так, как ему удобно.
    */
    /* HashSet<String> myHashSet = new HashSet<String>();
 myHashSet.add("eererer");
 myHashSet.add("ewewe");
 myHashSet.add("sdsdd");
 myHashSet.add("sdsdd");
 myHashSet.add("regvfdv");

 //myHashSet.remove("sdsdd");
 for (String i : myHashSet)
     System.out.println(i);

 //System.out.println("Does myHashSet contain 'ewewe'? " + myHashSet.contains("ewewe"));
 //System.out.println("Now myHashSet size is " + myHashSet.size());
 //myHashSet.clear();
 //System.out.println("After clear myHashSet size is " + myHashSet.size());
 //System.out.println(myHashSet.isEmpty());
}
}
*/
}
